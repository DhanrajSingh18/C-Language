#include<stdio.h>

int main(sum)
{	
	area=((l*h)/2)
	return area;
}

void main()
{
	int l,h,area,sum=0;
	printf("Enter l: ");
	scanf("%d",&l);
	printf("Enter h: ");
	scanf("%d",&h);
	
	sum=area;
	printf("sum of area is: %d ",area)
}
